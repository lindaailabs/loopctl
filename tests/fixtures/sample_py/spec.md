# Sample Project Spec

A minimal Python project used to exercise the loopctl local loop end-to-end.

## Conventions
- Pure functions in `calculator.py`.
- Tests live in `tests/` and run with `pytest`.

## Requirements for contributors
- Keep functions type-annotated.
- Add or extend tests for any behavior change.
